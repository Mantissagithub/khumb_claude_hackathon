# --- Core (required) -------------------------------------------------------
numpy>=1.24
Pillow>=10.0
PyYAML>=6.0
tqdm>=4.65
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
pandas>=2.0
scipy>=1.10
opencv-python-headless>=4.8     # augmentation + alignment

# --- Vector DB -------------------------------------------------------------
faiss-cpu>=1.8                  # use faiss-gpu on a CUDA box instead

# --- Embedding / detection -------------------------------------------------
# torch/torchvision: install the build matching your platform.
#   CPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchvision>=0.16

# Embedding backend — the pipeline auto-selects whichever is installed:
#   PREFERRED: InsightFace ArcFace (buffalo_l). Needs a C++ toolchain on Windows.
# insightface>=0.7.3
# onnxruntime>=1.16            # or onnxruntime-gpu on CUDA
#   FALLBACK (pure pip, rides on torch): facenet-pytorch (FaceNet/VGGFace2 + MTCNN)
facenet-pytorch>=2.5.3

# --- Optional detectors / extras ------------------------------------------
# retina-face>=0.0.17          # RetinaFace detector backend
# ultralytics>=8.3             # YOLOv11-face detector backend
# umap-learn>=0.5              # UMAP embedding plot (else t-SNE via sklearn)

# Notes:
#  * GPU + mixed precision: set device: cuda and mixed_precision: true in config.yaml.
#  * Everything is import-guarded; backends degrade gracefully when absent.
