# KumbhSeva — central API key store (TEMPLATE).
#
# 1. Copy this file to:  config/secrets.toml
# 2. Fill in your real keys there.
# config/secrets.toml is gitignored and must NEVER be committed.
#
# The admin app pre-fills the sidebar from these values; you can still override
# them in the UI at runtime.

[anthropic]
api_key = ""                 # sk-ant-...
model   = "claude-opus-4-8"

[supabase]
url      = ""                # https://xxxx.supabase.co
anon_key = ""                # the anon/public key
