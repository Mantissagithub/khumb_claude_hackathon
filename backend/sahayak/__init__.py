"""Sahayak (सहायक — 'helper') — the Kumbh reunification web app.

A role-based web app (Volunteer kiosks + Admin control room) served by the
FastAPI backend, with real-time cross-center match alerts over WebSocket.
Reuses the face_search engine for matching.
"""
